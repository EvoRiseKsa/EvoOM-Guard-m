# Source declarations for the trusted CI/release toolchain.
#
# Keep product tooling synchronized with pyproject.toml's build-system and
# [project.optional-dependencies].dev declarations. Direct compatibility imports
# used only by repository tooling are declared separately below. The generated
# ci.lock is the only Python dependency input consumed by CI/release.
setuptools==84.0.0
# Direct Python 3.10 fallback imported by tools/packaging/build_distribution.py.
tomli==2.4.1
pytest>=8
ruff>=0.6
mypy>=1.10
coverage>=7
cryptography>=41
jsonschema>=4.23
